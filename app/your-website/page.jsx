import YourWebsite from "../../components/YourWebsite";

export const metadata = {
  title: "Your Website — propose an upgraded design system",
  description:
    "Paste your site URL. We extract your current design, keep what works, and propose React components, a contrast-checked palette, fonts, and spacing — plus a new DESIGN.md.",
};

export default function YourWebsitePage() {
  return <YourWebsite />;
}
